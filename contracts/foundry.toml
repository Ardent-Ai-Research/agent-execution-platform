[profile.default]
src = "."
out = "out"
libs = ["lib"]
solc = "0.8.28"

remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/",
    "@account-abstraction/=lib/account-abstraction/",
]

# Compilation
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = false
